# Runtime dependencies. Pinned to the versions this was developed against.
requests==2.32.3
beautifulsoup4==4.12.3

# job_scraper/filters.py only, and there it earns its place: both filters are
# vectorised str.count/str.contains over the whole frame plus a seeded
# groupby().sample() for the per-company exemption cap, which is a real loop
# and a real reservoir sample in plain Python. (The claim this comment used to
# make -- "main_scraper.py only, to write one CSV that csv.DictWriter would
# also write" -- was already false: main_scraper does not import pandas.
# db/db_connection.py did, to reimplement a UNIQUE constraint with iterrows();
# that is now INSERT OR IGNORE over csv.DictReader.)
pandas==2.2.3

# cv_generator/docx_gen.py only. The alternative was string-substituting
# word/document.xml through zipfile, which works for the scalar placeholders --
# each one sits alone in a single <w:t> run -- but not for the three that
# expand into many paragraphs with bold lead-in runs. That is hand-written
# OOXML, and hand-written OOXML is what this library exists to not be.
python-docx==1.1.2

# Development.
pytest==8.3.4
flake8==7.1.1

# A JS renderer stays deliberately unpinned and uninstalled, and
# main_scraper.py must keep running on a machine with no browser. The seam is
# now wired -- job_scraper/render.py, reached only via --render, which imports
# playwright inside the flag branch -- but it stays an opt-in extra, never a
# hard requirement. The generic and API-discovery strategies were measured
# against the fixture corpus first: listing-page JSON-LD (0 of 34 boards),
# JOB_PATH tuning (the misses have no anchors to match), link pagination (1 of
# 5 multi-page boards), and Algolia key lifting (1 board, no key) were each
# rejected on the numbers. Rendering is what is left.
#
# playwright        # opt-in: --render only. Then: playwright install chromium
